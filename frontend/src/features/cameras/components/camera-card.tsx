import { useEffect, useRef, useState } from 'react';
import { Camera as CameraIcon, MoreVertical, Play, WifiOff } from 'lucide-react';
import Hls from 'hls.js';
import { formatDate } from '../../../shared/lib/formatters';
import type { Camera } from '../../../shared/types/api';
export function CameraCard({ camera, onSelect }: { camera: Camera; onSelect: (camera: Camera) => void }) { return <article className="camera-card" onClick={() => onSelect(camera)} role="button" tabIndex={0} onKeyDown={event => { if (event.key === 'Enter' || event.key === ' ') onSelect(camera); }}><div className="feed"><div className="feed-top"><span className={`status ${camera.status}`}>{camera.status === 'online' ? 'LIVE' : 'OFFLINE'}</span><span className="cam-id">CAM-{String(camera.id).padStart(2, '0')}</span></div>{camera.status === 'online' ? <CameraStream camera={camera} compact/> : <div className="offline"><WifiOff size={30}/><span>Cámara desconectada</span><small>{camera.last_seen ? `Última conexión: ${formatDate(camera.last_seen)}` : 'Sin conexión registrada'}</small></div>}<button className="expand" aria-label="Abrir cámara" onClick={event => { event.stopPropagation(); onSelect(camera); }}><Play size={14}/></button></div><div className="card-info"><div><h3>{camera.name}</h3><p>{camera.location}</p></div><MoreVertical size={17} className="more"/></div></article>; }
export function CameraStream({ camera, compact = false }: { camera: Camera; compact?: boolean }) { const videoRef = useRef<HTMLVideoElement>(null); const [state, setState] = useState<'loading'|'ready'|'error'>('loading'); useEffect(() => { const video = videoRef.current; if (!video || camera.status !== 'online') return; let hls: Hls | undefined; const onReady = () => setState('ready'); const onError = () => setState('error'); video.addEventListener('loadeddata', onReady); video.addEventListener('error', onError); if (video.canPlayType('application/vnd.apple.mpegurl')) video.src = camera.stream_url; else if (Hls.isSupported()) { hls = new Hls({ enableWorker: true, lowLatencyMode: true }); hls.on(Hls.Events.MANIFEST_PARSED, () => { setState('ready'); video.play().catch(() => undefined); }); hls.on(Hls.Events.ERROR, (_event, data) => { if (data.fatal) setState('error'); }); hls.loadSource(camera.stream_url); hls.attachMedia(video); } else setState('error'); return () => { video.removeEventListener('loadeddata', onReady); video.removeEventListener('error', onError); hls?.destroy(); video.pause(); video.removeAttribute('src'); video.load(); }; }, [camera]); return <div className={`scene ${compact ? 'compact' : ''}`}><video ref={videoRef} className="live-video" autoPlay muted playsInline controls={!compact}/>{state === 'loading' && <div className="stream-message"><CameraIcon size={34}/><span>Conectando al stream…</span></div>}{state === 'error' && <div className="stream-message error"><WifiOff size={30}/><span>Stream no disponible</span><small>{camera.stream_url}</small></div>}</div>; }
