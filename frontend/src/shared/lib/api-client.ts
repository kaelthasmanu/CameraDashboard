import type { AuthUser, Camera, Recording } from '../types/api';
const API_URL = (import.meta.env.VITE_API_URL ?? 'http://localhost:8000/api/v1').replace(/\/$/, '');
async function request<T>(path: string, init: RequestInit = {}): Promise<T> { const headers = new Headers(init.headers); const token = localStorage.getItem('access_token'); if (token) headers.set('Authorization', `Bearer ${token}`); const response = await fetch(`${API_URL}${path}`, { ...init, headers }); if (!response.ok) throw new Error(`La API respondió con ${response.status}`); return response.json() as Promise<T>; }
export const api = { login: async (username: string, password: string) => { const body = new URLSearchParams({ username, password }); const result = await request<{ access_token: string }>('/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/x-www-form-urlencoded' }, body }); localStorage.setItem('access_token', result.access_token); }, me: () => request<AuthUser>('/auth/me'), logout: () => localStorage.removeItem('access_token'), cameras: () => request<Camera[]>('/cameras'), recordings: (day: string, cameraId?: number) => request<Recording[]>(`/recordings?day=${encodeURIComponent(day)}${cameraId ? `&camera_id=${cameraId}` : ''}`), recordingStream: (id: number) => `${API_URL}/recordings/${id}/stream` };
export { API_URL };
